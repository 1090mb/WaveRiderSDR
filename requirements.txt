numpy>=1.21.0
matplotlib>=3.4.0
scipy>=1.7.0
pyserial>=3.5

# Desktop GUI dependencies (optional - for desktop version only)
PyQt5>=5.15.0

# Web interface dependencies (optional - for web/mobile version)
flask>=2.3.0
flask-socketio>=5.3.0
python-socketio>=5.9.0

# SDR hardware support (optional)
pyrtlsdr>=0.2.9

# Audio output support (optional - for audio demodulation and playback)
# Install at least one of these for audio playback:
sounddevice>=0.4.6  # Recommended for most platforms
# pyaudio>=0.2.13  # Alternative audio library (uncomment if preferred)
